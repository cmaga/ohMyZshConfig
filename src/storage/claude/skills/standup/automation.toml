# Standup config.
# Standup is now a manual weekday ritual (you invoke `/standup`), so the scheduled
# 12:30 auto-run is disabled. The skill still reads repo_path below; the Sunday
# timesheet reconciler invokes the skill headless to backfill any unposted day.
# Edit repo_path when you switch active projects.
# Only `cron`, `repo_path`, and `enabled` are read by the deploy script.

enabled = false
cron = "30 12 * * 1-5"
repo_path = "/Users/cmagana/dev/gsi/symage/symdocs/symdocs"
