def merge(intervals):
    """Merge overlapping intervals and return them sorted by start.

    intervals is a list of [start, end] pairs (integers, start <= end).
    Intervals that merely touch (e.g. [1,2] and [2,3]) DO overlap and must
    merge into [1,3]. Input may be unsorted. Return [] for empty input.
    Do not mutate the input list."""
    raise NotImplementedError
