"""Small statistics helpers. Implement each function per its docstring."""


def median(nums):
    """Return the median of a non-empty list of numbers.
    For even length, return the average of the two middle values (a float).
    Raise ValueError on empty input."""
    raise NotImplementedError


def mode(nums):
    """Return the most frequent value. On a tie, return the smallest such value.
    Raise ValueError on empty input."""
    raise NotImplementedError


def rolling_mean(nums, k):
    """Return the list of averages of every contiguous window of length k.
    Result has len(nums) - k + 1 elements. If k > len(nums), return [].
    Raise ValueError if k <= 0."""
    raise NotImplementedError
